# 現在の作業コンテキスト

**最終更新:** 2026-05-22

## 今いるフェーズ
**W1完了 → W2開始直前（2026-05-25〜）**

W1（セットアップ）は全員完了。来週から本実装フェーズに入る。

## 各担当の現在地

### しなり（Prgr A）
- W1: Git repo・Microgame fork・全員clone確認 → **完了**
- 次のタスク（5/25〜）: 移動・ジャンプパラメータ調整、キャラ切り替え基礎実装

### ポテト（Prgr B）
- W1: Microgame動作確認・担当箇所洗い出し → **進行中**
- 次のタスク（5/25〜）: ザコマセイモ巡回AIパターン

### デンちょス（Dsgn A）
- W1: キャラデザイン方向決め（3キャラ） → **進行中**
- 次のタスク（5/25〜）: ケン（切る）の待機・走りアニメ（4コマ）

### てくてく（Dsgn B）
- W1: タイルセット素材選定 → **進行中**
- 次のタスク（5/25〜）: ステージ1背景・タイル差し替え

## 直近の完了済み作業（技術）
- サンプルアニメ（Player8本・Enemy4本）を 12/48fps → **4fps** に変換済み
- `.clinerules/` 整備完了（00-safety / 10-commit / 20-approach / 30-structure）
- `.gitignore` に `*.bak` 追加済み

## 現在の懸念・注意事項
- `.gitignore` にて `*.bak` を HEADに追加済みだが、作業ツリー上で差分が出ている（要確認）
- Git LFSが未インストール環境（サンドボックス）でのgit操作は制限あり → **commitはしなりのWindows環境から実施**
- LFSバイナリ（png/wav）が unstaged modified として常に表示されるが、**実際には未変更**

## 次のセッションで着手すること
1. キャラ切り替えの基礎実装（`PlayerController.cs` 拡張）
2. コンボ入力受付の設計（属性フラグ管理）
3. デンちょスのスプライトが来たらアニメーション差し替え手順を整備
